
export default function Page({ params }) {
    const projectId = params.id
    // TODO: Use fetch to get project information from database

    return (
        <section>
            
        </section>
    )
}
